import { DISHES } from '../shared/dishes';



export const Dishes = (state = DISHES, action) => {
    //console.log(state)
    switch (action.type) {
        default:
          return state;
            
        }
      
};